import gsap from "gsap";
import { ScrollTrigger, SplitText } from "gsap/all";
gsap.registerPlugin( ScrollTrigger, SplitText)
const App = () => {
  return <div className="min-h-[100vh] flex-center">
    App
  </div>;
};

export default App;
